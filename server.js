// server.js has been removed per project simplification.
// The original server implementation was saved to `scripts/old_server.js`.
// If you need to run a backend, restore that file to project root as `server.js`.
console.log('server.js is intentionally disabled in this branch.');
